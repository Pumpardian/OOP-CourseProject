<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="ACS_Reception.UI.Pages.RecordDetailsPage"
             xmlns:converters="clr-namespace:ACS_Reception.UI.ValueConverters"
             xmlns:models="clr-namespace:ACS_Reception.UI.ViewModels"
             xmlns:entities="clr-namespace:ACS_Reception.Domain.Entities;assembly=ACS_Reception.Domain"
             x:Name="RecordDetails"
             x:DataType="models:RecordDetailsPageViewModel"
             Title="">

    <ContentPage.Resources>
        <ResourceDictionary Source="/Styles/CollectionStyle.xaml"/>
    </ContentPage.Resources>

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
            EventName="Appearing"
            Command="{Binding BindingContext.UpdateSerializationStatusCommand, Source={x:Reference RecordDetails}}" />
    </ContentPage.Behaviors>

    <ContentPage.MenuBarItems>
        <MenuBarItem Text="Record">
            <MenuFlyoutItem Text="Delete" Command="{Binding DeleteRecordCommand}" />
        </MenuBarItem>
        
        <MenuBarItem Text="Serialization" IsEnabled="{Binding IsSerializationPluginConnected}">
            <MenuFlyoutSubItem Text="Serialize">
                <MenuFlyoutItem Text="Json" Command="{Binding SerializeJsonCommand}"/>
                <MenuFlyoutItem Text="Xml" Command="{Binding SerializeXmlCommand}"/>
            </MenuFlyoutSubItem>
        </MenuBarItem>
    </ContentPage.MenuBarItems>

    <ScrollView Style="{StaticResource Scroll}">
        <StackLayout Spacing="10">
            <Border Style="{StaticResource InfoBorder}">
                <VerticalStackLayout>
                    <HorizontalStackLayout Style="{StaticResource CollectionItemLayout}">
                        <Label Text="Type:" Style="{StaticResource CollectionItemText}"/>
                        <Label Text="{Binding CardRecord.GetRecordName}" Style="{StaticResource CollectionItemText}"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Style="{StaticResource CollectionItemLayout}">
                        <Label Text="Date:" Style="{StaticResource CollectionItemText}"/>
                        <Label Text="{Binding CardRecord.Date, StringFormat='{0}'}" Style="{StaticResource CollectionItemText}"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Style="{StaticResource CollectionItemLayout}">
                        <Label Text="Doctor:" Style="{StaticResource CollectionItemText}"/>
                        <Label Text="{Binding CardRecord.Doctor}" Style="{StaticResource CollectionItemText}"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Style="{StaticResource CollectionItemLayout}">
                        <Label Text="Record Info:" Style="{StaticResource CollectionItemText}"/>
                        <Label Text="{Binding CardRecord.GetInfo}" Style="{StaticResource CollectionItemText}"/>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>
        </StackLayout>
    </ScrollView>
    
</ContentPage>