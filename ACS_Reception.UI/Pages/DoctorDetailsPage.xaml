<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="ACS_Reception.UI.Pages.DoctorDetailsPage"
             xmlns:converters="clr-namespace:ACS_Reception.UI.ValueConverters"
             xmlns:models="clr-namespace:ACS_Reception.UI.ViewModels"
             xmlns:entities="clr-namespace:ACS_Reception.Domain.Entities;assembly=ACS_Reception.Domain"
             x:Name="DoctorDetails"
             x:DataType="models:DoctorDetailsPageViewModel"
             Title="">

    <ContentPage.Resources>
        <ResourceDictionary Source="/Styles/CollectionStyle.xaml"/>
    </ContentPage.Resources>

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
            EventName="Appearing"
            Command="{Binding BindingContext.GetDoctorByIdCommand, Source={x:Reference DoctorDetails}}" />
        <toolkit:EventToCommandBehavior
            EventName="Appearing"
            Command="{Binding BindingContext.UpdateSerializationStatusCommand, Source={x:Reference DoctorDetails}}" />
    </ContentPage.Behaviors>

    <ContentPage.MenuBarItems>
        <MenuBarItem Text="Doctor">
            <MenuFlyoutItem Text="Edit" Command="{Binding EditDoctorCommand}" />
            <MenuFlyoutItem Text="Delete" Command="{Binding DeleteDoctorCommand}" />
        </MenuBarItem>
        
        <MenuBarItem Text="Serialization" IsEnabled="{Binding IsSerializationPluginConnected}">
            <MenuFlyoutSubItem Text="Serialize">
                <MenuFlyoutItem Text="Json" Command="{Binding SerializeJsonCommand}"/>
                <MenuFlyoutItem Text="Xml" Command="{Binding SerializeXmlCommand}"/>
            </MenuFlyoutSubItem>
        </MenuBarItem>
    </ContentPage.MenuBarItems>

    <ScrollView Style="{StaticResource Scroll}">
        <StackLayout Spacing="10">
            <Border Style="{StaticResource InfoBorder}">
                <VerticalStackLayout>
                    <HorizontalStackLayout Style="{StaticResource CollectionItemLayout}">
                        <Label Text="Type:" Style="{StaticResource CollectionItemText}"/>
                        <Label Text="{Binding Doctor.DoctorType}" Style="{StaticResource CollectionItemText}"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Style="{StaticResource CollectionItemLayout}">
                        <Label Text="Last Name:" Style="{StaticResource CollectionItemText}"/>
                        <Label Text="{Binding Doctor.LastName}" Style="{StaticResource CollectionItemText}"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Style="{StaticResource CollectionItemLayout}">
                        <Label Text="First Name:" Style="{StaticResource CollectionItemText}"/>
                        <Label Text="{Binding Doctor.FirstName}" Style="{StaticResource CollectionItemText}"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Style="{StaticResource CollectionItemLayout}">
                        <Label Text="Cabinet Number:" Style="{StaticResource CollectionItemText}"/>
                        <Label Text="{Binding Doctor.CabinetNumber, StringFormat='{0}'}" Style="{StaticResource CollectionItemText}"/>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>
        </StackLayout>
    </ScrollView>

</ContentPage>